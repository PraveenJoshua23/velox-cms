FROM directus/directus:11.5.1

VOLUME ["/directus/database", "/directus/uploads", "/directus/extensions"]

ENV SECRET=${DIRECTUS_SECRET}
ENV ADMIN_EMAIL=${DIRECTUS_ADMIN_EMAIL}
ENV ADMIN_PASSWORD=${DIRECTUS_ADMIN_PASSWORD}
ENV DB_CLIENT=${DIRECTUS_DB_CLIENT}
ENV DB_FILENAME=${DIRECTUS_DB_FILENAME}
ENV WEBSOCKETS_ENABLED=${DIRECTUS_WEBSOCKETS_ENABLED}

EXPOSE 8055